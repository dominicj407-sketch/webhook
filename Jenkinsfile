pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'GitHub triggered Jenkins! hello loe'
            }
        }

        stage('Build') {
            steps {
                echo 'Build is running...'
            }
        }
    }
}
