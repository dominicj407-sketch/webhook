pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'GitHub triggered Jenkins! hello loe hsudh'
            }
        }

        stage('Build') {
            steps {
                echo 'Build is running...'
            }
        }
    }
}
